namespace math {
    public class Matrix {
        // Constructor
        public Matrix() {
            Row = 0;
            Col = 0;
        }
        public Matrix(int row = 0, int col = 0) {
            Row = row;
            Col = col;
        }
        // Initialize

        // Properties
        public int Row { get; set; } = 0;
        public int Col { get; set; } = 0;
        // public long[,] Value { set; } = new long[0,0];

        // Getter

        // Setter

        // Operator

    }
}