namespace math {
    public class Matrix {
        // Constructor

        // Initialize

        // Properties
        public int Row { get; set; } = 0;
        public int Col { get; set; } = 0;
        public long[,] Value { set; } = new long[Row,Col];

        // Getter

        // Setter

        // Operator

    }
}